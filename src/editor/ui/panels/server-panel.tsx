import { NodeEditor } from "~/editor/node-editor";
import { NodeServerClient } from "~/network/websocket-handler";

export const ServerPanel = (props: {client: NodeServerClient, editor: NodeEditor}) => {
    
    return (
        <div class="server-panel">
            
        </div>
    );
}